<% current_user = User.find_by_id(session[:user_id]) %>

<%= form_tag url_for(controller: :home,action: :profile_update), multipart: true do %>
	<img src="/uploads/<%=current_user.profile_picture_name %>">
	<%= file_field_tag("profile_picture") %>
	<%= submit_tag("Update") %>
<% end %>